FROM node:22-alpine

WORKDIR /app

# Copy package configuration
COPY package.json package-lock.json* ./

# Copy source code
COPY . .

# Expose port
EXPOSE 5173

# Install deps at startup (so anonymous volume node_modules is populated) then run dev server
CMD ["sh", "-c", "npm install && npm run dev -- --host"]
